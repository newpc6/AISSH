# AI SSH 开发计划文档

## 1. 文档目的

本文档用于定义 AI SSH 的开发执行计划、里程碑拆分、交付物、工程规则和更新流程。

它必须始终与项目实际进度保持同步。任何较大的范围、架构、优先级或交付顺序变化，都需要体现在本文档中。

## 2. 工作约定

项目执行规则：

1. 每次里程碑调整或范围调整，都要更新本文档。
2. 如果调整同时影响产品设计或架构方向，需要同步更新 `docs/product-design.md`。
3. 每完成一批明确改动，都要进行一次清晰的 git 提交。
4. 不允许实现长期跑在文档前面，计划与实际要尽量同步。

当前仓库说明：

- 仓库已初始化 git
- 当前阶段已完成 Milestone 0，正在推进 Milestone 1 / Milestone 2 的可用性，并提前建设 Milestone 3 的 AI 配置入口
- Rust / Cargo 环境已可用
- Tauri 桌面壳已接入现有前端工程，并完成 `cargo check` 与 `tauri info` 验证
- 当前已完成 React + TypeScript 前端骨架、Go core health server、共享契约包与本地联调配置
- 当前已进入 SSH / SFTP MVP 可用性打磨阶段，并推进 Milestone 3 的基础能力。已实现主机列表、独立分组配置、空分组保存、服务器卡片菜单、服务器编辑/删除、会话接口、SSE 输出流、input 写入接口、input 顺序队列、PTY resize 同步、会话 tab 关闭、tab 状态圆点、断开后重连、每会话终端输出缓存、关闭后自动切换或回到空状态、无会话时隐藏伪 tab/连接状态、基于远端 shell 回显最终命令的历史记录、终端控制序列过滤、备用屏全屏程序历史隔离、被动 prompt 路径跟踪、会话当前路径兜底读取、`local-demo` 终端演示链路、临时密码认证 SSH 连接、工作台主布局、固定视口布局、左侧可拖动宽度、顶部下拉菜单、文件菜单分类型导入导出、含加密凭据的服务器列表迁移、设置弹窗分栏与保存提示、左侧新增 SSH 连接弹窗、SSH Key 文件选择、保存失败弹窗内反馈、保存成功自动选中新服务器、服务器基础配置本地 JSON 持久化、密码 / SSH Key 系统安全存储、Go core 请求日志、前端运行日志面板、日志级别设置与前端筛选、AI provider 失败诊断日志、SSE 连接失败状态反馈、SFTP 文件列表/路径输入/上级目录/上传/下载首版、远程文本文件 CodeMirror 预览/编辑/保存与工具栏、首次启动登录密码设置、初始化不依赖桌面 token、登录/初始化密码显示切换、桌面端启动登录开关、认证后终端延迟初始化、服务器 CPU 当前使用率、内存已用 / 总量、CPU / 内存带坐标趋势图、悬浮点位提示与放大查看、指标圆点默认值与配置、当前服务器卡片折叠、磁盘列表、网络实时带宽首版、图标按钮悬浮提示、OpenAI 兼容 AI 命令预测、Go core HTTP 与远端 SSH/SFTP 模块拆分，以及 API 405 自动回退诊断

## 3. 交付策略

整体按阶段推进：

1. 先证明桌面壳与 SSH 核心架构方向是可行的
2. 再交付真正可用的 SSH + 文件传输 MVP
3. 在 SSH 核心体验稳定后叠加 AI 能力
4. 最后做稳定性、打磨和发布准备

## 4. 里程碑

## Milestone 0：项目启动与骨架搭建

目标：

- 建立仓库结构、工程脚手架、编码规范和架构基线

交付物：

- 基础目录结构
- 前端工程初始化
- Go core 服务初始化
- 桌面壳方案设计落地
- 基础 IPC / API 契约
- lint / format / test 基础命令
- CI 基线
- 产品设计与开发计划文档

建议目录结构：

- `apps/desktop`
- `apps/core-go`
- `packages/shared-contracts`
- `docs/`

当前阶段说明：

- 文档基线已完成
- 当前已形成“前端 + Tauri 桌面壳 + Go core”的基础三层骨架
- 下一步进入 SSH 会话管理与终端容器开发

退出标准：

- 前端项目可运行
- Go core 可运行
- 前端可以通过最小接口拿到 core 的健康检查结果
- 目录结构、基础脚本、文档都已提交

## Milestone 1：SSH 核心连接能力

目标：

- 交付稳定的 SSH 连接与终端流能力

交付物：

- Host 数据模型与本地持久化
- 密码 / 私钥 / agent 认证支持
- known_hosts 校验流程
- 终端标签管理
- xterm.js 集成
- SSH 会话打开 / 关闭 / 重连
- 基础命令历史

退出标准：

- 用户可以保存主机并打开交互式 shell
- 终端足够稳定，可支撑日常命令执行
- 断开、重连状态清晰可控

## Milestone 2：文件管理与传输

目标：

- 让远程文件操作变得高频可用

交付物：

- 远程文件浏览器
- 上传 / 下载
- 拖拽传输
- 传输进度展示
- 失败重试与错误反馈
- 基础文件操作

退出标准：

- 用户可以稳定浏览远程文件并进行上传下载
- 常见传输失败有清晰反馈并可恢复

## Milestone 3：AI 副驾驶基础能力

目标：

- 加入真正有用、可控、安全的 AI 能力

交付物：

- AI 提供方抽象层
- Provider / Model / API Key 设置
- 输出 / 错误解释能力
- 自然语言转命令草稿
- 命令风险分级
- AI 审计记录

退出标准：

- 用户可以让 AI 解释输出或生成命令草稿
- AI 默认不会自动执行命令

## Milestone 4：交互打磨与生产力增强

目标：

- 让产品具备长期日常使用的顺手感

交付物：

- 分屏能力
- 快捷命令 / 片段
- 更完整的键盘快捷键
- 主题和视觉细节打磨
- 空状态 / 错误状态优化
- 传输管理器 UI 改进
- 主机与会话搜索筛选

退出标准：

- 产品适合日常重度使用

## Milestone 5：稳定性加固与发布准备

目标：

- 做好可靠性、打包、观测和发布准备

交付物：

- 崩溃恢复机制
- 更完善的结构化日志
- 可选遥测埋点
- 打包与签名流程
- 更新策略
- 性能优化
- 安全检查清单

退出标准：

- 能稳定产出跨平台候选版本

## 5. 当前立即执行任务

当前建议按以下顺序推进：

1. 完成文档中文化并保持基线一致
2. 完成基础目录结构
3. 完成 React + TypeScript 前端工程初始化
4. 完成 Go core 服务初始化
5. 完成最小共享契约：
   - health check
   - host 基础结构
   - session 基础结构
6. 完成前端到 core 的最小健康检查链路
7. 完成 Tauri 桌面壳接入与基础验证
8. 完成主机列表、会话接口与终端容器首版
9. 完成 SSE 输出流和 input 写入接口
10. 完成临时密码认证 SSH 登录
11. 完成顶部菜单、左侧 SSH/文件模式、中间终端 tabs、右侧 AI/历史命令工作台布局
12. 完成左侧新增 SSH 连接弹窗，并让右侧专注于服务器信息、AI 工具和历史命令
13. 完成服务器新增、导入导出
14. 完成服务器基础配置本地 JSON 持久化
15. 完成系统安全存储保存密码和 SSH Key
16. 完成 Go core 请求日志、前端运行日志面板、日志级别设置与 API 405 自动回退诊断
17. 完成服务器卡片菜单、编辑、删除与会话输出流失败状态反馈
18. 完成顶部下拉菜单、设置弹窗、会话 tab 关闭和真实命令历史记录
19. 完成 SFTP 文件列表、上传、下载与基础进度展示首版
20. 完成当前服务器指标轮询展示与刷新频率设置
21. 完成 AI 提供方配置入口与命令预测交互占位
22. 修复命令历史截断、移除路径跟踪命令注入、优化 tab 状态/关闭切换和无会话空状态
23. 拆分 Go core HTTP 路由与远端 SSH/SFTP 模块，继续使用 Go 标准库 `net/http`
24. 优化文件目录路径输入、左侧宽度拖动、服务器指标趋势和文件菜单导入导出分组
25. 调整服务器配置默认存储路径，并支持含加密凭据的服务器列表导出/导入迁移
26. 修复无会话空状态噪音、历史命令控制序列污染、文件路径跟踪兜底和远端指标解析
27. 增加 SSH 分组独立配置、空分组保存和新增连接分组选择
28. 支持断开会话重连、每会话终端缓存、键盘级命令历史记录，以及可放大的 CPU / 内存趋势图
29. 优化指标卡片展示、内存已用/总量、趋势图悬浮详情和图标按钮悬浮提示
30. 修复历史命令回车提交解析、SSH 输入顺序和 PTY 尺寸同步，改善 vim 等全屏程序显示
31. 接入真实 OpenAI 兼容 AI provider 命令预测、右侧服务器卡片折叠和设置分栏
32. 修复 SFTP 文件目录初始路径显示，空路径、`.` 和相对路径会解析为远端真实绝对路径
33. 将 AI 命令预测发送给大模型的系统提示词和用户提示词模板改为中文
34. 优化 AI 预测可见状态、右侧工具条目稳定高度、终端自适应高度、`cd` 路径跟踪兜底和传输任务固定显示
35. 增加桌面端默认窗口高度，并让右侧 AI / 历史命令区域随剩余高度自适应
36. 优化远程文件加载状态，将加载提示收进刷新按钮左侧旋转图标
37. 修复终端命令历史 / `cd` 路径推断解析，并优化 AI 预测候选展示与 Tab 循环交互
38. 支持传输任务记录关闭，并在中间标签页打开远程文件预览
39. 固定远程文件列表行高，避免文件数量少时文件和文件夹卡片被拉伸
40. 将命令历史记录改为优先解析远端 shell prompt 回显的最终命令，修复 Tab 补全后历史残缺问题
41. 将 AI 预测提示移动到 xterm 光标位置，并明确预测上下文长度、历史条数和模型请求限制
42. 将 AI 预测上下文字符数和历史命令条数做成可保存的系统配置项，并保留默认值
43. 修复旧运行状态下 AI 预测上下文字符数和历史命令条数设置项显示空白的问题
44. 修复 Go core 重启或会话输入失败后前端 SSH 状态仍显示已连接的问题，并支持旧 session 丢失后的快速重连
45. 增加 AI 预测和历史命令收藏功能，支持收藏列表展示与快速填充终端
46. 收藏列表支持删除收藏命令，并使用应用内确认弹窗避免误删
47. 修复运行日志弹窗按所选级别筛选展示，并增强 AI provider 预测失败时的详细诊断日志
48. 收紧 AI 预测解析兜底逻辑，拒绝半截 JSON / `commands` 结构残片被误当成可执行命令
49. 右侧全局错误提示增加来源、接口、状态码和详情，并在同接口后续请求成功时自动清除
50. 进一步在后端命令清洗和前端展示前过滤 AI 预测结构化残片，并为运行日志增加关键字搜索
51. 优化 AI 预测请求约束与截断诊断，提高输出 token、要求 JSON object、记录 `finish_reason` / `reasoning_content`，并在前端显示预测中旋转状态
52. 收藏命令列表支持手动添加、序号展示和上移 / 下移排序
53. 远程文本文件预览改为 CodeMirror，默认只读预览，支持切换编辑并保存回远端
54. Go core 支持托管前端 Web、网页登录认证和服务器命令行部署；桌面端启动时尝试拉起内置 core，浏览器可访问同一服务
55. 优化收藏命令排序按钮为上下布局，减少横向占用
56. 远程文件预览头部动作横向排布，并为 CodeMirror 编辑模式增加工具栏
57. 修复登录页后终端未初始化导致 SSH 内容空白的问题，改为认证通过后再初始化 xterm
58. Go core 首次启动不再自动生成随机网页登录密码，改为由用户设置密码，并支持桌面客户端启动是否需要登录的安全设置
59. 修复首次初始化误用桌面 token 导致 `desktop token invalid` 的问题，并为登录 / 初始化密码框增加显示隐藏按钮
60. AI 预测、历史命令和收藏命令卡片增加执行按钮，点击后直接向当前 SSH 会话发送命令并回车执行
61. CodeMirror 编辑工具栏接入编辑器命令 API，提供撤销、重做、全选、缩进、反缩进和注释等操作，并保留跨格式文本格式化入口
62. 修复文件预览 tab 与 SSH 会话 tab 切换：文件预览不再销毁 xterm，切回任意 SSH 会话可恢复原终端内容，文件 tab 标明所属服务器
63. 增加一键打包脚本和中文打包说明，支持生成桌面客户端安装包、Web 发布目录与 Web 压缩包
64. 桌面客户端文件目录接入系统拖入上传和远程文件拖出下载
65. 修复桌面端未要求登录时仍进入登录页、拖入上传重复进度和拖出下载弹保存对话框的问题
66. 继续推进 agent 认证与 known_hosts 校验

## 6. 工程规则

### 架构规则

- SSH / SFTP 逻辑放在 Go core，不直接放在前端
- 前端与 core 的通信契约必须显式定义、可演进
- AI provider 逻辑不要直接耦合进终端 UI 组件

### 产品规则

- AI 建议必须经过用户审核后才可执行
- host key 校验不能被静默跳过
- 敏感凭据优先使用操作系统安全存储

### 文档规则

- 范围、优先级、阶段变化必须同步更新本文档
- 若架构假设变化，需要同步更新产品设计文档
- 每次提交前检查文档是否仍和实现一致

## 7. 计划跟踪

当前状态：

- Milestone 0：已完成
- Milestone 1：进行中
- Milestone 2：进行中
- Milestone 3：进行中（已接入 OpenAI 兼容命令预测，解释/审计等能力待完善）
- Milestone 4：未开始
- Milestone 5：未开始

当前检查项：

- [x] 明确产品方向
- [x] 明确开发里程碑
- [x] 初始化 git 仓库
- [x] 创建工程目录结构
- [x] 初始化前端工程
- [x] 初始化 Go core
- [x] 打通健康检查链路
- [x] 接入 Tauri 桌面壳
- [x] 开始 SSH 会话管理
- [x] 完成终端容器首版
- [x] 完成会话事件流与终端输入写入链路
- [x] 接入临时密码认证 SSH 登录
- [x] 完成工作台主布局重构
- [x] 完成左侧新增 SSH 连接弹窗
- [x] 支持新增连接时选择 SSH Key 文件
- [x] 完成新增连接保存反馈、成功选中与双击直连
- [x] 完成服务器列表基础管理
- [x] 接入服务器配置本地持久化
- [x] 接入系统安全存储保存密码和 SSH Key
- [x] 接入私钥认证
- [x] 接入 Go core 请求日志与运行日志接口
- [x] 增加前端工具菜单运行日志面板和日志级别设置
- [x] 增加前端 API 405 自动回退与保存主机失败诊断日志
- [x] 增加服务器卡片菜单、编辑、删除与复制配置入口
- [x] 修复日志中间件破坏 SSE 输出流的问题
- [x] 增加会话输出流失败时的前端错误状态反馈
- [x] 顶部菜单改为下拉菜单，并将日志入口放入工具菜单
- [x] 支持会话 tab 关闭确认与后端会话关闭接口
- [x] 记录终端中输入的真实命令历史
- [x] 修复历史命令截断问题，只记录回车提交的完整命令
- [x] 路径跟踪改为被动识别常见 shell prompt，不再向远端自动注入 `printf` 探测命令
- [x] 优化会话 tab 关闭按钮、状态圆点、关闭后自动切换和全部关闭后的空状态
- [x] 固定主界面高度，页面本身不产生全局滚动，左右侧内容独立滚动
- [x] 接入 SFTP 文件列表、目录进入、上传、下载和基础传输进度
- [x] 接入当前服务器 CPU、内存、硬盘、网络指标首版
- [x] 支持文件目录路径输入回车进入
- [x] 左侧资源栏默认加宽并支持拖动调整宽度
- [x] 修复左侧服务器列表少量数据时不贴顶显示的问题
- [x] 当前服务器面板支持 CPU / 内存折线趋势、多个磁盘挂载点和网络实时上下行带宽
- [x] 顶部右侧移除重复导入导出按钮，文件菜单按服务器列表和软件配置分组导入导出
- [x] 默认将 `hosts.json` 保存到 Go core 当前工作目录的 `data/hosts.json`，并支持 `AI_SSH_HOME` / `AI_SSH_HOSTS_PATH` 覆盖
- [x] 支持服务器列表含加密凭据导出，导入后恢复密码/SSH Key 到目标电脑系统安全存储
- [x] 无会话时不显示“未连接”伪 tab 和终端头部连接状态，只保留空状态快捷连接
- [x] 历史命令过滤终端控制序列，记录数量扩展并支持右侧独立滚动
- [x] 文件目录支持上级目录按钮，并在切换文件页时通过会话当前路径接口兜底刷新
- [x] 服务器指标改为读取远端 `/proc` 与 `df` 原始数据后由 Go 解析，修复网络 32 位溢出等假值问题
- [x] 偏好设置增加保存按钮与保存成功提示
- [x] SSH 分组作为独立本地配置保存，支持新建空分组、管理分组和新增连接时选择已有分组
- [x] 支持 SSH 会话断开后重连，并在当前标签恢复连接
- [x] 为每个 SSH 标签保留独立终端输出缓存，默认保留最新 1000 行并支持设置调整
- [x] 将历史命令记录从终端输出污染中剥离，避免普通输出和控制序列混入历史
- [x] 历史命令解析改为以后端远端 shell 回显最终命令为准，并在 vim 等备用屏全屏程序中暂停记录
- [x] SSH 输入请求按会话排队发送，避免快速输入或全屏程序操作时乱序
- [x] 同步 xterm 实际行列到后端 PTY，改善 vim 等全屏程序滚动和状态栏显示错位
- [x] CPU / 内存趋势图增加 0-100% 纵轴、时间横轴和放大查看
- [x] CPU / 内存趋势图支持悬浮查看具体时间点、使用率和内存已用/总量
- [x] CPU / 内存卡片改为顶部信息、下方图表的上下布局，并将放大入口改为图表右上侧无边框图标
- [x] CPU / 内存小图保留完整折线，但按比例只显示少量圆点，避免点位过密影响观感
- [x] 指标图圆点数量支持偏好设置，小图和放大图可分别配置，默认值分别为 5 和 20
- [x] 当前服务器卡片支持折叠和展开
- [x] 为图标按钮和紧凑按钮补充悬浮文字提示
- [x] 设置弹窗拆分为通用、服务器指标和 AI 预测多个菜单
- [x] 增加设置弹窗，支持服务器指标刷新频率、终端保留行数和 AI provider 配置
- [x] 接入真实 OpenAI 兼容 AI provider 命令预测，支持可配置预测数量，默认 3 条
- [x] AI 预测首条命令作为默认候选，并在终端区域以浅灰提示展示
- [x] 拆分 Go core HTTP API 与 SSH/SFTP 远端能力模块，降低 `server.go` 体积
- [x] 修复 SFTP 文件目录初始路径显示，空路径、`.` 和相对路径会在列目录时解析为远端真实绝对路径
- [x] AI 命令预测发送给大模型的系统提示词和用户提示词模板统一使用中文
- [x] 修复回车后立即取消 AI 预测的问题，并在 AI 面板显示启用、加载、缺配置和空结果状态
- [x] 右侧 AI 预测 / 历史命令单条卡片保持稳定高度，工具区域随右侧剩余空间自适应
- [x] 终端区域跟随容器尺寸变化自动 fit，窗口变大后 SSH 内容区同步铺满
- [x] 文件路径跟踪增加 `cd` 命令本地推断兜底，并继续由 SFTP 真实路径响应校正
- [x] 文件传输任务固定显示在左侧文件面板底部，客户端下载优先使用 Tauri 原生保存对话框选择保存地址
- [x] 桌面客户端默认窗口高度增加 100 像素，启动后提供更高工作区
- [x] 远程文件加载状态改为刷新按钮左侧旋转图标，不再占用文件列表单独一行
- [x] 命令历史改为基于远端 shell prompt 回显解析，`cd` 路径推断继续使用 xterm 输入流做即时兜底
- [x] AI 面板移除固定快捷建议，只在有真实预测时展示候选；空命令行按 Tab 循环预测候选，回车执行当前候选
- [x] 传输任务记录支持右上角关闭，便于清理已完成或失败的传输记录
- [x] 单击远程文件可在中间工作区新开文件预览标签，支持文本、图片、视频预览和大文件打开确认
- [x] 固定远程文件列表中文件和文件夹卡片高度，少量文件时仍贴顶显示不拉伸
- [x] 命令历史改为使用后端从远端 shell 回显中解析出的最终命令，并保留重复执行记录
- [x] AI 预测提示跟随 xterm 光标位置展示，并将前端 5000 字符上下文、20 条历史和 Go core 50000 字符、200 条历史兜底限制写入设计文档
- [x] AI 预测上下文字符数和历史命令条数支持在设置中配置，默认分别为 5000 和 20
- [x] 修复旧设置或热更新状态下 AI 预测上下文字符数、历史命令条数输入框默认值显示为空的问题
- [x] Go core 重启、SSE 关闭、输入或 resize 失败时，前端会将 SSH 会话标记为已断开，并可用原主机快速重连替换当前 tab
- [x] AI 预测和历史命令支持星标收藏，收藏命令可在右侧收藏列表中查看、取消收藏并快速填充终端
- [x] 收藏列表支持删除收藏命令，并使用应用内确认弹窗避免误删
- [x] 运行日志弹窗按所选级别过滤展示，选择 `error` 时只展示错误级别日志
- [x] AI provider 预测失败时以错误级别记录状态码、响应体片段、模型输出片段和解析失败原因，便于在运行日志中定位 `no commands` 等问题
- [x] AI 预测解析拒绝半截 JSON 和 `commands` 结构残片，避免把 `{"commands":["docker ps -a` 展示成命令建议
- [x] 右侧全局错误提示展示功能来源、接口路径、状态码、详情和时间，并在对应请求后续成功时自动消失
- [x] AI 预测在 Go core 清洗层和前端展示前双重过滤结构化残片，运行日志弹窗支持按关键字搜索日志内容和字段
- [x] AI 预测请求要求 JSON object、提高输出 token、明确必须预测且由用户人工确认，并在模型输出截断时记录 `finish_reason` 和 reasoning 片段
- [x] AI 预测面板在调用大模型期间显示旋转图标和预测中状态
- [x] 收藏命令列表支持手动添加、显示序号、上移 / 下移排序和原有删除确认
- [x] 远程文本文件预览使用 CodeMirror，默认预览模式，可切换编辑并保存回远端，显示保存成功或失败
- [x] Go core 支持托管前端 Web 并保护 API 登录态，桌面端启动时可拉起内置 core，服务器上可仅启动 core 提供网页登录访问
- [x] 收藏命令上移 / 下移按钮改为上下布局，减少横向占用并扩大命令显示区域
- [x] 远程文件预览头部将预览、编辑、保存和下载保持同一行，CodeMirror 编辑模式增加格式化、还原和复制工具栏
- [x] 认证通过后再初始化 xterm，避免首次登录页占位导致 SSH 会话内容区域为空
- [x] Go core 首次启动未设置网页登录密码时进入初始化流程，并在安全设置中支持桌面客户端启动是否要求登录
- [x] 首次初始化改用公开初始化接口，避免调试或 core 复用时桌面 token 不一致导致 `desktop token invalid`
- [x] 登录页与初始化页密码框支持显示 / 隐藏，便于确认首次设置密码
- [x] AI 预测、历史命令和收藏命令卡片新增独立执行按钮，点击后直接输入当前终端并回车执行
- [x] CodeMirror 编辑模式工具栏接入 CodeMirror 命令 API，提供撤销、重做、全选、缩进、反缩进和注释等编辑动作
- [x] 文件预览 tab 与 SSH 会话 tab 可自由切换，打开文件后切回原会话或其他会话都恢复对应终端缓存
- [x] 文件预览 tab 和文件预览头部显示所属服务器，避免多服务器文件混淆
- [x] 增加 `scripts/package-release.ps1`，可一键打包桌面客户端、Web 发布目录和 Web zip
- [x] 增加中文打包说明文档，并在 README 中加入入口
- [x] 桌面客户端文件目录支持系统文件拖入上传，并支持远程文件拖出时通过下载 URL 交给系统拖放目标处理
- [x] 修复桌面端未要求登录时仍进入登录页、拖入上传重复创建进度任务和远程文件拖出直接弹保存对话框的问题
- [x] 远程文件列表改为单击选择、Ctrl/Shift 多选、双击预览/进入目录，并在文件面板顶部提供选中文件批量下载入口
- [x] 桌面端批量下载可选择本地保存目录，并通过 Tauri 命令把多个远程文件写入该目录，传输任务展示整体进度
- [x] 固定 SSH / 文件预览标签栏高度，横向滚动只发生在标签栏内部，避免打开较多标签时挤压标签内容
- [x] Windows 桌面端远程文件拖出在 WebView2 未接管下载时，会尝试识别鼠标所在的资源管理器窗口并下载到该文件夹作为兜底
- [ ] 评估 Tauri/Rust 原生文件 promise，继续增强不同平台拖出下载到系统目标文件夹的兼容性
- [ ] 接入 agent 认证
- [ ] 接入 known_hosts 严格校验

## 8. Git 提交约定

推荐提交风格：

- `docs: 中文化产品设计与开发计划`
- `chore: 初始化前端与 Go core 工程骨架`
- `feat(core): 增加 ssh 会话管理器`
- `feat(files): 增加 sftp 上传下载`
- `feat(ai): 增加命令解释能力`
- `docs(plan): 更新里程碑顺序`

规则：

- 任何影响范围或顺序的改动，都需要把计划文档更新包含在同一次提交或相邻提交中

## 9. 风险与应对

### 风险：本机代理或依赖环境影响 Rust 生态拉包

应对：

- 在仓库中保留 Cargo 配置
- 明确记录代理与依赖拉取问题
- 保持前端、桌面壳、Go core 三层可独立验证

### 风险：AI 功能喧宾夺主

应对：

- 在 SSH 与文件管理体验可用前，不提前推进大量 AI 功能

### 风险：安全债务积累

应对：

- 将密钥管理、host 校验、命令风险控制前置到 MVP

### 风险：界面打磨拖慢核心能力

应对：

- 先保证主流程可用，再逐步做高质量打磨

## 10. 文档变更规则

当实现方向发生变化时：

1. 先更新本文档，或与实现一起更新
2. 如果架构假设变化，同时更新 `docs/product-design.md`
3. 文档变更与相关代码一并提交
4. 保持里程碑状态准确
